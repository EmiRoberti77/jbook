{
  "name": "jsnote-emi",
  "version": "1.0.0",
  "description": "",
  "bin": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "files":[
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --watch --preserveWatchOutput",
    "prepublishOnly": "esbuild src/index.ts --platform=node --outfile=dist/index.js --bundle --minify --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@jsnote-emi/local-client": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "esbuild": "0.8.26",
    "typescript": "^4.9.3",
    "commander": "^9.4.1",
    "@jsnote-emi/local-api": "^1.0.0"
  }
}
